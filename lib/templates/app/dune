(executable 
 (public_name ##[cl,pname]) 
 (name ##[cl,pname])
 (promote (until-clean))
 (libraries camlp-streams prelude etude lib)) 

(env
 (dev
  (flags (:standard -warn-error -A)))
 (release
  (ocamlopt_flags (:standard))
  (link_flags (:standard -cclib -static))))

; Local Variables:
; mode: dune
; End:
